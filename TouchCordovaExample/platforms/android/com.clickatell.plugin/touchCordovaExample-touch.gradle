repositories {
    maven { url "$rootDir/src/touch-sdk-android" }
}

dependencies {
    compile('com.clickatell.chatsecure.androidsdk:touch-android-sdk:+')
}

android {
    buildTypes {
        release {
            minifyEnabled false
        }
	debug {
            minifyEnabled false
	}
    }

}
